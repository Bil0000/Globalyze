export function MarketingHero() {
  return (
    <section>
      <p>Ship once. Launch everywhere.</p>
      <h1>Global releases without the localization backlog</h1>
      <p>
        Detect hardcoded strings, refactor JSX automatically, and generate locale
        files from one CLI command.
      </p>
      <div>
        <button>Book a demo</button>
        <button>View CLI workflow</button>
      </div>
    </section>
  );
}
